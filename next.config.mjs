/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "media.themoviedb.org",
      "www.kliknusae.com",
      "dp3a.mataramkota.go.id",
    ],
  },
};

export default nextConfig;
